# 个人配置文件里是学号和密码，任何分支都绝对不要提交
config.ini
config.toml

# 日志
logs/*.log

# main 分支只放项目说明与许可证，不放代码。
# 切换分支后工作区里会残留其他分支的文件，下面这些规则确保 git add -A 不会
# 把它们误提交到本分支（main 的跟踪文件只有 .gitignore / LICENSE / README.md）。
BitSrunLogin/
login_once.py
always_online.py
config.py
logger.py
manage.py
selftest.py
autoConnectNetwork.bat
config.example.toml
config.example.ini

# powershell 分支的脚本
uestc-login.ps1
install-task.ps1
install-service.ps1
uninstall.ps1

# 各分支的运行产物
__pycache__/
python-path.txt
UESTC-AutoLogin.xml
